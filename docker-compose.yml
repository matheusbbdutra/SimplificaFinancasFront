services:
  frontend:
    build: .
    container_name: vue-frontend
    volumes:
    - .:/app:cached
    - /app/node_modules
    - /app/.nuxt
    ports:
      - "3000:3000"
    networks:
      - symfony

networks:
  symfony:
    external: true
